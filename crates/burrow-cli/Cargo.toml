[package]
name = "burrow-cli"
description = "Command-line disk usage report, and a check of the engine's sizes against Windows."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ferret-core = { path = "../ferret-core" }
burrow-tree = { path = "../burrow-tree" }
windows-sys = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
] }

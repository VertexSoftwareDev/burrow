[workspace]
resolver = "2"
members = [
    "crates/ferret-core",
    "crates/ferret-tree",
    "crates/ferret-disk-cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
